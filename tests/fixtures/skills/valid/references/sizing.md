# Sizing

A spot buy has a $2 floor, $25 on Ethereum mainnet. Under it, tell your owner the
number instead of filing the order. A tokenized stock's floor is per ticker:

```sh
bevo-read stock-min <TICKER>
```
